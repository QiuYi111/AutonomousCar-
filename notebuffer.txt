int right_Rpm_length=0,left_Rpm_length=0;
		cRt[0]='l';
		cRt[1]=':';
		left_Rpm_length=float_to_uint8_arry(cRt+2, currentRpm_left, 2);
		cRt[left_Rpm_length+3]=' ';
		cRt[left_Rpm_length+4]='r';
		cRt[left_Rpm_length+5]=':';
		right_Rpm_length=float_to_uint8_arry(cRt+left_Rpm_length+6, currentRpm_right, 2);
		cRt[left_Rpm_length+right_Rpm_length+7]='\n';

        int distanceFront_length=0,distanceLeft_length=0,distanceRight_length=0;
		cRt[left_Rpm_length+right_Rpm_length+8]='F';
		cRt[left_Rpm_length+right_Rpm_length+9]=':';
		distanceFront_length=float_to_uint8_arry(cRt+left_Rpm_length+right_Rpm_length+10, (float)distanceFront, 0);
		cRt[distanceFront_length+left_Rpm_length+right_Rpm_length+11]='\n';
		cRt[distanceFront_length+left_Rpm_length+right_Rpm_length+12]='L';
		cRt[distanceFront_length+left_Rpm_length+right_Rpm_length+13]=':';
		distanceLeft_length=float_to_uint8_arry(cRt+distanceFront_length+left_Rpm_length+right_Rpm_length+14, (float)distanceLeft, 0);
		cRt[distanceLeft_length+distanceFront_length+left_Rpm_length+right_Rpm_length+15]='\n';
		cRt[distanceLeft_length+distanceFront_length+left_Rpm_length+right_Rpm_length+16]='R';
		cRt[distanceLeft_length+distanceFront_length+left_Rpm_length+right_Rpm_length+17]=':';
		distanceRight_length=float_to_uint8_arry(cRt+distanceLeft_length+distanceFront_length+left_Rpm_length+right_Rpm_length+18, (float)distanceRight, 0);
		cRt[distanceRight_length+distanceLeft_length+distanceFront_length+left_Rpm_length+right_Rpm_length+19]='\n';
		HAL_UART_Transmit_DMA(&huart5, cRt, sizeof cRt);
